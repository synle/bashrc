// extensions that should open in the Ghostty terminal on macOS
// .command files are macOS executable shell scripts (double-click in Finder
// runs them in a terminal). Ghostty must be installed; mapping is applied via
// duti by software/scripts/mac/file-association.js.
command
